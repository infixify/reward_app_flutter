name: reward_app_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.4

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^3.6.0
  firebase_messaging: ^15.1.3

  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.17.2
  device_info_plus: ^13.2.0
  google_sign_in: ^7.2.0
  google_mobile_ads: ^9.1.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
